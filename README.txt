Project renamed to Task

- class Point now has only X and Y coordinates
- classes Figure FigureGenerator, /*Figure is base class for all new classes except FigureGenerator/* , Rectangle, Triangle, Circle,  are created
- class FigureGenerator is generator of random figures realized with methods GeneratePoint(), GenerateRectangle(), GenerateTriangle(), GenerateCircle()
